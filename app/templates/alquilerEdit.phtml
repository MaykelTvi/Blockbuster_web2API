<?php require './app/templates/header.phtml'; ?>

<h1>Editar Alquiler</h1>
<form method="post" action="?action=actualizarAlquiler/<?= $idAlquiler ?>">

    <label>Película</label>
    <select name="id_pelicula" class="form-control mb-2" required>
        <?php foreach($peliculas as $p): ?>
            <option value="<?= $p->id_pelicula ?>" <?= $p->id_pelicula == $alquiler->id_pelicula ? 'selected' : '' ?>>
                <?= htmlspecialchars($p->titulo) ?>
            </option>
        <?php endforeach ?>
    </select>

    <label>Usuario</label>
    <select name="id_usuario" class="form-control mb-2" required>
        <?php foreach($usuarios as $u): ?>
            <option value="<?= $u->id_usuario ?>" <?= $u->id_usuario == $alquiler->id_usuario ? 'selected' : '' ?>>
                <?= htmlspecialchars($u->usuario) ?>
            </option>
        <?php endforeach ?>
    </select>

    <label>Fecha de alquiler</label>
    <input type="date" name="fecha_alquiler" class="form-control mb-2" value="<?= $alquiler->fecha_alquiler ?>" required>

    <button type="submit" class="btn btn-success">Actualizar</button>
    <a href="?action=alquileres" class="btn btn-secondary">Cancelar</a>
</form>

<?php require './app/templates/footer.phtml'; ?>
