<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <base href="<?= BASE_URL ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/estilos.css">
    <title>Blockbuster Digital</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand text-warning fw-bold" href="?action=home">🎬 BLOCKBUSTER</a>
                <div class="collapse navbar-collapse">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link text-white" href="?action=peliculas">PELÍCULAS</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="?action=alquileres">ALQUILERES</a>
                        </li>
                        <?php if (isset($_SESSION['id_usuario']) && isset($_SESSION['admin']) || 
                                  isset($_SESSION['id_usuario'])): ?>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="?action=usuarios">USUARIOS</a>
                        </li>
                        <?php endif; ?>
                    </ul>
                    <ul class="navbar-nav">
                        <?php if (!isset($_SESSION['id_usuario'])): ?>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="?action=login">Iniciar sesión</a>
                        </li>
                        <?php else: ?>
                        <li class="nav-item">
                            <span class="nav-link text-warning">👤 <?= htmlspecialchars($_SESSION['usuario']) ?></span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white" href="?action=logout">Cerrar sesión</a>
                        </li>
                        <?php endif; ?>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main class="container mt-3">
